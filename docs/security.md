# Security

_To be filled in during Phase 6 (DevSecOps)._

Planned coverage:
- Why each CI/CD scanning stage exists (SAST, dependency scan, container scan)
- Trivy image scanning config
- Secret scanning
- RBAC design for the AKS cluster and for the AI agent's tool access
- Azure Key Vault usage (no secrets in env vars/ConfigMaps)
- Least-privilege rationale for every service identity in the system
