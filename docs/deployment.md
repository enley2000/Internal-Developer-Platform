# Deployment

_To be filled in during Phase 3 (Kubernetes/Helm) and Phase 4 (CI/CD)._

Planned coverage:
- How to deploy the Helm chart to AKS
- Environment promotion (dev → staging → prod)
- Rollback procedure (`kubectl rollout undo`, and how the AI agent proposes it)
- Required environment variables / secrets per environment
